services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - composer_cache:/root/.composer/cache
    environment:
      PHP_IDE_CONFIG: "serverName=adf-converter"
      XDEBUG_MODE: "off"

volumes:
  composer_cache:
